% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classify_lc_to_palm.R
\name{classify_lc_to_palm}
\alias{classify_lc_to_palm}
\title{Reclassify LC raster to PALM surface type rasters}
\usage{
classify_lc_to_palm(data, nodata_out = 255, mapping = NULL)
}
\arguments{
\item{data}{A \code{terra::SpatRaster} object with LC classification codes.}

\item{nodata_out}{The output NoData value (default: 255).}

\item{mapping}{Optional reclassification table.  Can be:
\itemize{
\item \code{NULL} (default) to use the built‑in rules described below;
\item a filename pointing to a text file (CSV/TSV) containing columns
\code{surface}, \code{src}, and \code{dst};
\item a data.frame with the same three columns.  The \code{surface} column
indicates which of the three PALM raster layers (\code{vegetation},
\code{water}, \code{pavement}) the row applies to.
}}
}
\value{
A named list of three \code{terra::SpatRaster} objects:
\itemize{
\item \code{vegetation}: Vegetation type classification
\item \code{water}: Water type classification
\item \code{pavement}: Pavement type classification
}
}
\description{
Converts a Land Cover raster to PALM-compatible vegetation, water,
and pavement type rasters using reclassification rules.
}
\details{
When \code{mapping = NULL} the following default rules are applied:
\itemize{
\item Vegetation: LC 4→3, 5→1, 8→1, 9→16, 10→17, 11→7
\item Water: LC 2→1
\item Pavement: LC 12→1, 6→13
}

A user‑provided mapping overrides these defaults.  An example of a CSV
file suitable for \code{mapping}:

\if{html}{\out{<div class="sourceCode csv">}}\preformatted{surface,src,dst
vegetation,4,3
vegetation,5,1
vegetation,8,1
vegetation,9,16
vegetation,10,17
vegetation,11,7
water,2,1
pavement,12,1
pavement,6,13
}\if{html}{\out{</div>}}
}
\examples{
\dontrun{
lc <- rast("LC.tif")

# use built-in rules
palm_surfaces <- classify_lc_to_palm(lc)

# or read a simple conversion table
conv <- read.csv("lc_to_palm.csv")
palm_surfaces <- classify_lc_to_palm(lc, mapping = conv)

# file path also works
palm_surfaces <- classify_lc_to_palm(lc, mapping = "lc_to_palm.csv")

plot(palm_surfaces$vegetation)
plot(palm_surfaces$water)
plot(palm_surfaces$pavement)
}

}
